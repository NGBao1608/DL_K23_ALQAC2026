# Colab installs keep the preinstalled torch/CUDA build unchanged.
# The notebook removes the unused Gradio UI stack, including hf-gradio, because
# its huggingface-hub>=1 dependency conflicts with the Transformers 4.x runtime.
python-dotenv>=1.0,<2
pyyaml>=6.0,<7
requests>=2.31,<3
tqdm>=4.66,<5
numpy>=1.26,<2.4
scikit-learn>=1.4,<2
pyvi>=0.1.1
rank-bm25>=0.2.2,<1
sentence-transformers>=3.0,<6
transformers>=4.51,<5
accelerate>=0.33,<2
bitsandbytes>=0.43,<1
peft>=0.13,<1
