# --- Core ---
python-dotenv>=1.0
pyyaml>=6.0
tqdm>=4.66
numpy>=1.26
pandas>=2.1
scikit-learn>=1.4

# --- Vietnamese NLP / sparse retrieval ---
pyvi>=0.1.1            # word segmentation (PhoBERT-style)
underthesea>=6.8       # NER for masking party names (optional but recommended)
rank_bm25>=0.2.2

# --- Dense retrieval / rerank ---
sentence-transformers>=3.0
FlagEmbedding>=1.2     # BGE-M3 + bge-reranker
faiss-cpu>=1.8         # use faiss-gpu if you have CUDA

# --- LLM inference (open-weight, <10B params ONLY) ---
torch>=2.3
transformers>=4.44
accelerate>=0.33
vllm>=0.5              # high-throughput local inference; needs a GPU
# bitsandbytes>=0.43   # uncomment for 4-bit quant on small GPUs

# --- Submission / API ---
requests>=2.31

# --- Experiment tracking (optional) ---
# wandb>=0.17
